{
  "devDependencies": {
    "@types/workbox-sw": "^4.2.1",
    "typescript": "^3.6.3",
    "workbox-sw": "^4.3.1"
  },
  "scripts": {
    "build": "rm -rf ./dist & mkdir dist & cp -r ./templates/* ./dist & tsc",
    "start": "serve ./dist"
  }
}
